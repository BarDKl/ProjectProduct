[project]
name = "safe-to-spend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "faker>=40.37.0",
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
